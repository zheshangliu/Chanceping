{
  "name": "chanceping",
  "version": "0.8.0",
  "private": true,
  "description": "盯一下 ChancePing - AI 机会情报系统（V0.8 搜索层 + LLM 版）",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "verify": "tsx scripts/verify-task019.ts",
    "integration-test": "tsx scripts/integration-test.ts",
    "check:no-hardcode": "node scripts/check-no-hardcode.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "i18next": "^23.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4"
  }
}
