{
  "name": "chanceping",
  "version": "0.7.5",
  "private": true,
  "description": "盯一下 ChancePing - AI 机会情报系统（V0.7.5 集成验证版）",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "verify": "tsx scripts/verify-acceptance.ts",
    "integration-test": "tsx scripts/integration-test.ts",
    "check:no-hardcode": "node scripts/check-no-hardcode.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4"
  }
}
